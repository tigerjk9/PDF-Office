# PDF Office Phase 2 구현 계획 (2페이지 2-up 스프레드 + 페이지 패널 리사이즈)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 연속 뷰어에 2페이지(N행 2열) 스프레드 보기 모드를 추가하고, 좌측 페이지 썸네일 패널을 마우스 드래그로 폭 조절 + localStorage 영속화한다.

**Architecture:** 보기 모드를 `[연속|단일|2페이지]` 3종으로 확장. 2페이지는 별도 컴포넌트 없이 `ContinuousViewer`를 확장(순수 헬퍼 `groupIntoRows`로 행 그룹핑 + 열당 절반폭 박스). 1열 경로는 기존과 **동일 인자**로 호출해 회귀 0. 모드 전환 시 PageSlot 리마운트 → IntersectionObserver effect deps에 `viewMode`를 추가해 윈도잉 정지를 방지. 패널 리사이즈는 의존성 0의 자체 훅(`usePanelWidth`)+핸들(`PanelResizer`)로 구현, localStorage 영속.

**Tech Stack:** Next.js 16 App Router, React 19, TypeScript 5.6, Zustand 5(persist+IndexedDB), pdfjs-dist, Tailwind v3, lucide-react.

**검증 모델 (프로젝트 표준 — 테스트 러너 없음):** 이 프로젝트는 단위 테스트 러너가 없다(jest/vitest 부재, 추가 금지 = scope creep). `npm run lint`는 Next 16에서 사전 파손 상태이므로 게이트에서 제외한다. 각 코드 태스크의 검증은 **`npx tsc --noEmit`(0 errors)**, 통합 시 **`npm run build`(exit 0)**, UI 동작은 **Task 9의 헤드리스 도그푸딩**(Phase 1에서 사용자가 선택한 "일괄 검증" 패턴)으로 수용한다. 순수 함수의 런타임 정확성은 Task 9 도그푸딩 1번 항목(5p PDF → 행 1·2/3·4/5)으로 입증한다.

**선행 조건:** 작업 브랜치에서 진행(예: `feat/viewer-phase2`). 커밋은 태스크별 1개. 푸시·배포·프로덕션 폴링은 **사용자 별도 요청 시에만**(plan 범위 외).

---

### Task 1: ViewMode 타입에 'spread' 추가

**Files:**
- Modify: `src/lib/types.ts:145-146`

- [ ] **Step 1: 타입·주석 수정**

`src/lib/types.ts` 145-146행을 아래로 교체한다.

기존:
```ts
/** 뷰어 보기 모드. Phase 2에서 'spread'(2페이지) 추가 예정. */
export type ViewMode = 'continuous' | 'single'
```

신규:
```ts
/** 뷰어 보기 모드. 'spread'=2페이지 2-up(연속 스크롤 2열). */
export type ViewMode = 'continuous' | 'single' | 'spread'
```

- [ ] **Step 2: 타입 검사**

Run: `npx tsc --noEmit`
Expected: 0 errors (기존 `setViewMode: (mode: ViewMode) => void`, store `setViewMode`, migrate 모두 하위호환 — 'spread'는 유니온 확장이라 기존 사용처 깨지지 않음).

- [ ] **Step 3: 커밋**

```bash
git add src/lib/types.ts
git commit -m "feat: ViewMode 타입에 'spread'(2페이지) 추가"
```

---

### Task 2: groupIntoRows 순수 헬퍼 신규

**Files:**
- Create: `src/lib/pdf/spread.ts`

- [ ] **Step 1: 파일 생성**

`src/lib/pdf/spread.ts` 신규 작성:

```ts
/**
 * 연속 뷰어 2페이지(2-up) 행 그룹핑 — 순수 함수.
 *
 * 단순 2-up(표지 오프셋 없음): [0,1] / [2,3] / [4,5] …,
 * 홀수 끝 페이지는 단독 행 [iN].
 * cols<=1 이면 각 아이템을 단독 행([x])으로 반환해
 * 1열·2열 렌더 경로를 동일 "행 배열" 구조로 통일한다.
 * 순수·결정적(부수효과 없음).
 */
export function groupIntoRows<T>(items: T[], cols: number): T[][] {
  if (cols <= 1) return items.map((x) => [x])
  const rows: T[][] = []
  for (let i = 0; i < items.length; i += cols) {
    rows.push(items.slice(i, i + cols))
  }
  return rows
}
```

- [ ] **Step 2: 타입 검사**

Run: `npx tsc --noEmit`
Expected: 0 errors.

- [ ] **Step 3: 런타임 정합성(검증 모델 주석)**

이 프로젝트는 테스트 러너가 없으므로 별도 테스트 파일을 추가하지 않는다(scope creep 금지). 이 함수의 런타임 정확성은 **Task 9 도그푸딩 1번 항목**에서 5페이지 PDF를 `2페이지` 모드로 전환했을 때 행이 `1·2 / 3·4 / 5`(0-based `[0,1]/[2,3]/[4]`)로 그룹핑되는지로 입증한다. 기대 동작 명세:
- `groupIntoRows([0,1,2,3,4], 1)` → `[[0],[1],[2],[3],[4]]`
- `groupIntoRows([0,1,2,3,4], 2)` → `[[0,1],[2,3],[4]]`
- `groupIntoRows([], 2)` → `[]`

- [ ] **Step 4: 커밋**

```bash
git add src/lib/pdf/spread.ts
git commit -m "feat: groupIntoRows 2-up 행 그룹핑 순수 헬퍼"
```

---

### Task 3: ZoomControl에 '2페이지' 버튼 추가

**Files:**
- Modify: `src/components/viewer/ZoomControl.tsx:4-13` (import), `:240-241` (보기 모드 group 내 3번째 버튼)

- [ ] **Step 1: lucide 아이콘 import에 Columns2 추가**

`src/components/viewer/ZoomControl.tsx` 4-13행 import 블록을 교체한다.

기존:
```tsx
import {
  ChevronLeft,
  ChevronRight,
  ZoomIn,
  ZoomOut,
  Maximize2,
  MoveHorizontal,
  ScrollText,
  FileText,
} from 'lucide-react'
```

신규:
```tsx
import {
  ChevronLeft,
  ChevronRight,
  ZoomIn,
  ZoomOut,
  Maximize2,
  MoveHorizontal,
  ScrollText,
  FileText,
  Columns2,
} from 'lucide-react'
```

- [ ] **Step 2: 보기 모드 그룹에 3번째 버튼 추가**

`src/components/viewer/ZoomControl.tsx`에서 "한 페이지씩 보기" Tooltip 블록(`<TooltipContent>한 페이지씩 보기</TooltipContent></Tooltip>` — 239-240행)과 그 그룹 div 닫힘(`</div>` — 241행) 사이에 아래 Tooltip 블록을 삽입한다.

찾을 기준 텍스트(이 정확한 연속 라인 뒤에 삽입):
```tsx
          <TooltipContent>한 페이지씩 보기</TooltipContent>
        </Tooltip>
      </div>
```

위를 아래로 교체:
```tsx
          <TooltipContent>한 페이지씩 보기</TooltipContent>
        </Tooltip>
        <Tooltip>
          <TooltipTrigger asChild>
            <Button
              variant={viewMode === 'spread' ? 'default' : 'ghost'}
              size="icon"
              className="h-7 w-7"
              onClick={() => setViewMode('spread')}
              aria-label="두 페이지씩 보기"
              aria-pressed={viewMode === 'spread'}
            >
              <Columns2 className="h-4 w-4" />
            </Button>
          </TooltipTrigger>
          <TooltipContent>두 페이지씩 보기</TooltipContent>
        </Tooltip>
      </div>
```

(`viewMode`·`setViewMode`는 43-44행에서 이미 셀렉트되어 있어 추가 작업 불필요. fit-page 버튼은 264행 `{viewMode === 'single' && (...)}`로 감싸 있어 spread에서 자동 숨김 — 무변경.)

- [ ] **Step 3: 타입 검사**

Run: `npx tsc --noEmit`
Expected: 0 errors.

- [ ] **Step 4: 커밋**

```bash
git add src/components/viewer/ZoomControl.tsx
git commit -m "feat: 보기 모드에 '두 페이지씩 보기'(Columns2) 버튼 추가"
```

---

### Task 4: PdfViewer 셸 분기 변경 (single만 SinglePageViewer)

**Files:**
- Modify: `src/components/viewer/PdfViewer.tsx:7-15`

- [ ] **Step 1: 셸 분기·주석 교체**

`src/components/viewer/PdfViewer.tsx` 7-15행을 아래로 교체한다.

기존:
```tsx
/**
 * 뷰어 셸 — viewer.viewMode 에 따라 단일/연속 뷰어를 분기한다.
 * 단일 경로는 SinglePageViewer 로 동작 변경 없이 보존(회귀 최소화).
 */
export function PdfViewer() {
  const viewMode = usePdfStore((s) => s.viewer.viewMode)
  if (viewMode === 'continuous') return <ContinuousViewer />
  return <SinglePageViewer />
}
```

신규:
```tsx
/**
 * 뷰어 셸 — viewer.viewMode 에 따라 뷰어를 분기한다.
 * 'single'만 SinglePageViewer(동작 변경 없이 보존, 회귀 최소화),
 * 'continuous'·'spread'는 ContinuousViewer(spread=2페이지 2-up).
 */
export function PdfViewer() {
  const viewMode = usePdfStore((s) => s.viewer.viewMode)
  if (viewMode === 'single') return <SinglePageViewer />
  return <ContinuousViewer />
}
```

- [ ] **Step 2: 타입 검사**

Run: `npx tsc --noEmit`
Expected: 0 errors.

- [ ] **Step 3: 커밋**

```bash
git add src/components/viewer/PdfViewer.tsx
git commit -m "feat: 뷰어 셸 분기 — single만 단일, continuous/spread는 연속"
```

---

### Task 5: ContinuousViewer 2-up 스프레드 렌더

**Files:**
- Modify: `src/components/viewer/ContinuousViewer.tsx` (import 추가, SPREAD_GAP 상수, viewMode 셀렉터, cols, boxes useMemo, rows useMemo, IO effect deps, render 블록)

이 태스크는 spec §4.2 전체를 구현한다. **핵심**: IO effect deps에 `viewMode`를 반드시 추가(누락 시 모드 전환 후 윈도잉 정지 — 전 페이지 썸네일 고착). 1열 경로는 기존과 동일 인자로 호출(continuous 회귀 0).

- [ ] **Step 1: groupIntoRows import 추가**

`src/components/viewer/ContinuousViewer.tsx` 상단 import 영역에서 다음 줄
```tsx
import { useViewerScrollSync } from '@/hooks/useViewerScrollSync'
```
바로 아래에 추가:
```tsx
import { groupIntoRows } from '@/lib/pdf/spread'
```

- [ ] **Step 2: SPREAD_GAP 상수 추가**

기존:
```tsx
/** 뷰포트 기준 ±이만큼의 화면을 미리 렌더(프리페치) */
const PREFETCH_SCREENS = 1.5
```

아래로 교체:
```tsx
/** 뷰포트 기준 ±이만큼의 화면을 미리 렌더(프리페치) */
const PREFETCH_SCREENS = 1.5

/** 2페이지(2-up) 열 사이 간격(px) — Tailwind gap-4=16 와 일치 */
const SPREAD_GAP = 16
```

- [ ] **Step 3: viewMode 셀렉터 추가**

기존:
```tsx
  const activeDoc = usePdfStore(selectActiveDoc)
  const zoom = usePdfStore((s) => s.viewer.zoom)
  const fitMode = usePdfStore((s) => s.viewer.fitMode)
  const currentPageIndex = usePdfStore((s) => s.viewer.currentPageIndex)
```

아래로 교체:
```tsx
  const activeDoc = usePdfStore(selectActiveDoc)
  const zoom = usePdfStore((s) => s.viewer.zoom)
  const fitMode = usePdfStore((s) => s.viewer.fitMode)
  const viewMode = usePdfStore((s) => s.viewer.viewMode)
  const currentPageIndex = usePdfStore((s) => s.viewer.currentPageIndex)
```

- [ ] **Step 4: cols 파생값 + boxes useMemo 교체**

기존:
```tsx
  // 페이지별 박스(연속 모드는 fit-page 미지원 → fit-width/zoom만; 컨테이너 p-6=48 보정)
  const pages = activeDoc?.pages ?? []
  const boxes = useMemo(() => {
    return pages.map((p) =>
      computePageBox(p, {
        zoom,
        fitMode: fitMode === 'fit-page' ? 'fit-width' : fitMode,
        availW: containerSize.w,
        availH: containerSize.h,
        pad: 48,
      }),
    )
  }, [pages, zoom, fitMode, containerSize.w, containerSize.h])
```

아래로 교체:
```tsx
  // 페이지별 박스(연속 모드는 fit-page 미지원 → fit-width/zoom만; 컨테이너 p-6=48 보정)
  // spread(2열): 열당 가용폭 perCol = floor((컨테이너폭 - p-6 48 - gap 16) / 2),
  // pad=0(perCol이 이미 padding 차감값). 1열은 기존과 동일 인자 → 회귀 0.
  const pages = activeDoc?.pages ?? []
  const cols = viewMode === 'spread' ? 2 : 1
  const boxes = useMemo(() => {
    const perCol = Math.floor((containerSize.w - 48 - SPREAD_GAP) / 2)
    return pages.map((p) =>
      computePageBox(p, {
        zoom,
        fitMode: fitMode === 'fit-page' ? 'fit-width' : fitMode,
        availW: cols === 2 ? perCol : containerSize.w,
        availH: containerSize.h,
        pad: cols === 2 ? 0 : 48,
      }),
    )
  }, [pages, zoom, fitMode, containerSize.w, containerSize.h, viewMode])
```

- [ ] **Step 5: rows useMemo 추가 (boxes 바로 아래)**

Step 4에서 교체한 boxes useMemo 블록 바로 아래(IntersectionObserver useEffect 주석 `// IntersectionObserver 윈도잉` 위)에 삽입:

```tsx
  // 2-up 행 그룹: 페이지 배열 위치 인덱스를 cols 단위로 묶는다.
  // cols=1 → [[0],[1],…](기존 연속과 동일 시각), cols=2 → [[0,1],[2,3],…].
  // boxes[i]는 pages[i]와 위치 정렬돼 있으므로 위치 인덱스로 그룹핑한다.
  const rows = useMemo(
    () => groupIntoRows(pages.map((_, i) => i), cols),
    [pages, cols],
  )
```

- [ ] **Step 6: IntersectionObserver effect deps에 viewMode 추가 (필수)**

`src/components/viewer/ContinuousViewer.tsx`의 IntersectionObserver useEffect 마지막 줄
```tsx
  }, [pages.length, activeDoc?.id])
```
를 아래로 교체(모드 전환 시 PageSlot 리마운트 → IO 재생성·재관찰 필요):
```tsx
    // viewMode 포함 필수: continuous↔spread 전환 시 행 그룹/키가 바뀌어
    // PageSlot이 리마운트된다. viewMode 미포함 시 IO가 재실행되지 않아
    // 리마운트된 새 슬롯을 관찰하지 못함 → 윈도잉 정지(전 페이지 썸네일 고착).
  }, [pages.length, activeDoc?.id, viewMode])
```

- [ ] **Step 7: render 블록을 행 레이아웃으로 교체**

기존(파일 하단 return 내부):
```tsx
      {pages.map((p, i) => (
        <PageSlot
          key={`${activeDoc.id}-${p.index}`}
          page={p}
          box={boxes[i]}
          doc={doc}
          pageIndex={p.index}
          visible={visible.has(p.index)}
          bytes={activeDoc.bytes}
        />
      ))}
```

아래로 교체:
```tsx
      {rows.map((row) => (
        <div
          key={`row-${activeDoc.id}-${row[0]}`}
          className="flex w-full flex-row items-start justify-center gap-4"
        >
          {row.map((i) => {
            const p = pages[i]
            return (
              <PageSlot
                key={`${activeDoc.id}-${p.index}`}
                page={p}
                box={boxes[i]}
                doc={doc}
                pageIndex={p.index}
                visible={visible.has(p.index)}
                bytes={activeDoc.bytes}
              />
            )
          })}
        </div>
      ))}
```

(PageSlot·box 인자·doc·bytes·data-page-index 무변경. cols=1이면 행당 PageSlot 1개 → 외곽 `flex-col gap-4 items-center` + 행 `w-full justify-center`로 기존과 동일 시각·동일 `[data-page-index]` 타깃이라 IO·scroll-sync 무영향.)

- [ ] **Step 8: 타입 검사**

Run: `npx tsc --noEmit`
Expected: 0 errors.

- [ ] **Step 9: 커밋**

```bash
git add src/components/viewer/ContinuousViewer.tsx
git commit -m "feat: ContinuousViewer 2-up 스프레드 렌더 (rows 그룹·perCol 박스·IO viewMode deps)"
```

---

### Task 6: usePanelWidth 훅 신규

**Files:**
- Create: `src/hooks/usePanelWidth.ts`

- [ ] **Step 1: 파일 생성**

`src/hooks/usePanelWidth.ts` 신규 작성:

```ts
'use client'

import { useEffect, useState } from 'react'

/**
 * 패널 너비 상태 + localStorage 영속 + 클램프.
 *
 *  - 초기 렌더는 SSR 안전하게 def 사용(서버/클라 동일).
 *  - 마운트 후 effect에서 localStorage 값 읽어 클램프 반영(있을 때만).
 *  - setWidth는 [min,max] 클램프 후 state + localStorage 동기화.
 *  - 손상값(비유한)은 무시하고 기존값 유지(폴백).
 */
export function usePanelWidth(
  key: string,
  def: number,
  min: number,
  max: number,
): { width: number; setWidth: (w: number) => void } {
  const [width, setWidthState] = useState(def)

  useEffect(() => {
    try {
      const raw = localStorage.getItem(key)
      if (raw == null) return
      const parsed = Number(raw)
      if (!Number.isFinite(parsed)) return
      setWidthState(Math.max(min, Math.min(max, parsed)))
    } catch {
      /* localStorage 접근 불가(프라이빗 모드 등) → def 유지 */
    }
  }, [key, min, max])

  const setWidth = (w: number) => {
    if (!Number.isFinite(w)) return
    const clamped = Math.max(min, Math.min(max, w))
    setWidthState(clamped)
    try {
      localStorage.setItem(key, String(clamped))
    } catch {
      /* 저장 실패 무해(메모리 상태는 유지) */
    }
  }

  return { width, setWidth }
}
```

- [ ] **Step 2: 타입 검사**

Run: `npx tsc --noEmit`
Expected: 0 errors.

- [ ] **Step 3: 커밋**

```bash
git add src/hooks/usePanelWidth.ts
git commit -m "feat: usePanelWidth 훅 (localStorage 영속·클램프)"
```

---

### Task 7: PanelResizer 컴포넌트 신규

**Files:**
- Create: `src/components/layout/PanelResizer.tsx`

- [ ] **Step 1: 파일 생성**

`src/components/layout/PanelResizer.tsx` 신규 작성:

```tsx
'use client'

import { useRef } from 'react'

/**
 * 패널 우측 경계 드래그 리사이저(수직 분리자).
 *
 *  - pointerdown: 시작 X·너비 캡처 + setPointerCapture, body user-select 차단.
 *  - pointermove: onWidthChange(startW + dx). 클램프는 호출측(setWidth) 책임.
 *  - pointerup/cancel: 캡처 해제·드래그 종료, user-select 복원.
 */
export function PanelResizer(props: {
  width: number
  min: number
  max: number
  onWidthChange: (w: number) => void
}) {
  const { width, onWidthChange } = props
  const dragRef = useRef<{ startX: number; startW: number } | null>(null)

  const onPointerDown = (e: React.PointerEvent<HTMLDivElement>) => {
    if (e.button !== 0) return
    e.preventDefault()
    dragRef.current = { startX: e.clientX, startW: width }
    e.currentTarget.setPointerCapture(e.pointerId)
    document.body.style.userSelect = 'none'
  }
  const onPointerMove = (e: React.PointerEvent<HTMLDivElement>) => {
    const d = dragRef.current
    if (!d) return
    onWidthChange(d.startW + (e.clientX - d.startX))
  }
  const endDrag = (e: React.PointerEvent<HTMLDivElement>) => {
    if (!dragRef.current) return
    dragRef.current = null
    try {
      e.currentTarget.releasePointerCapture(e.pointerId)
    } catch {
      /* 캡처 해제 실패 무해 */
    }
    document.body.style.userSelect = ''
  }

  return (
    <div
      className="absolute right-0 top-0 z-10 h-full w-1.5 cursor-col-resize bg-transparent transition-colors hover:bg-primary/40 active:bg-primary/40"
      onPointerDown={onPointerDown}
      onPointerMove={onPointerMove}
      onPointerUp={endDrag}
      onPointerCancel={endDrag}
      role="separator"
      aria-orientation="vertical"
      aria-label="페이지 패널 크기 조절"
    />
  )
}
```

- [ ] **Step 2: 타입 검사**

Run: `npx tsc --noEmit`
Expected: 0 errors.

- [ ] **Step 3: 커밋**

```bash
git add src/components/layout/PanelResizer.tsx
git commit -m "feat: PanelResizer 드래그 핸들 컴포넌트"
```

---

### Task 8: AppShell 페이지 패널 동적 width + PanelResizer 배치

**Files:**
- Modify: `src/components/layout/AppShell.tsx` (import 2줄, 훅 호출, 페이지 패널 div 343-356행)

- [ ] **Step 1: import 추가**

`src/components/layout/AppShell.tsx`에서 다음 줄
```tsx
import { AppFooter } from '@/components/layout/AppFooter'
```
바로 아래에 추가:
```tsx
import { PanelResizer } from '@/components/layout/PanelResizer'
import { usePanelWidth } from '@/hooks/usePanelWidth'
```

- [ ] **Step 2: usePanelWidth 훅 호출 추가**

`AppShell` 함수 본문에서 다음 줄
```tsx
  const canMerge = documents.length >= 2
```
바로 아래에 추가:
```tsx

  // 페이지 패널 폭(데스크탑) — 드래그 리사이즈 + localStorage 영속
  const { width: pagesPanelWidth, setWidth: setPagesPanelWidth } =
    usePanelWidth('pdf-office-pages-panel-w', 248, 180, 420)
```

- [ ] **Step 3: 데스크탑 페이지 패널 div 교체**

`src/components/layout/AppShell.tsx` 343-356행(데스크탑 페이지 패널)을 교체한다.

기존:
```tsx
                  <div className="hidden w-[15.5rem] flex-shrink-0 flex-col border-r border-border bg-background md:flex">
                    <div className="flex h-9 flex-shrink-0 items-center justify-between px-3">
                      <span className="text-2xs font-semibold uppercase tracking-[0.06em] text-muted-foreground">
                        페이지
                      </span>
                      <span className="text-2xs tabular-nums text-muted-foreground">
                        {activeDoc.pageCount}
                      </span>
                    </div>
                    <Separator />
                    <div className="min-h-0 flex-1 overflow-hidden">
                      <PageGrid onRequestDelete={requestDelete} />
                    </div>
                  </div>
```

신규:
```tsx
                  <div
                    className="relative hidden flex-shrink-0 flex-col border-r border-border bg-background md:flex"
                    style={{ width: pagesPanelWidth }}
                  >
                    <div className="flex h-9 flex-shrink-0 items-center justify-between px-3">
                      <span className="text-2xs font-semibold uppercase tracking-[0.06em] text-muted-foreground">
                        페이지
                      </span>
                      <span className="text-2xs tabular-nums text-muted-foreground">
                        {activeDoc.pageCount}
                      </span>
                    </div>
                    <Separator />
                    <div className="min-h-0 flex-1 overflow-hidden">
                      <PageGrid onRequestDelete={requestDelete} />
                    </div>
                    <PanelResizer
                      width={pagesPanelWidth}
                      min={180}
                      max={420}
                      onWidthChange={setPagesPanelWidth}
                    />
                  </div>
```

(모바일 `hidden`이 `md:flex`보다 우선 → 모바일에서 width 무관·드로어 무변경. `relative`로 `absolute` 리사이저 기준 생성. `flex-shrink-0`로 옆 뷰어가 커져도 폭 고정. 문서 사이드바·모바일 Sheet·overflow 구조 무변경.)

- [ ] **Step 4: 타입 검사**

Run: `npx tsc --noEmit`
Expected: 0 errors.

- [ ] **Step 5: 커밋**

```bash
git add src/components/layout/AppShell.tsx
git commit -m "feat: 페이지 패널 드래그 리사이즈 배선 (usePanelWidth+PanelResizer)"
```

---

### Task 9: 통합 빌드 + 헤드리스 도그푸딩 + 변경이력

**Files:**
- Modify: `CLAUDE.md` (변경 이력 표 행 추가)
- 검증 산출물(임시 PDF 등)은 커밋하지 않음.

이 태스크는 spec §6 완료 기준 6항목을 일괄 검증한다(Phase 1에서 사용자가 선택한 "일괄 검증" 패턴). 도그푸딩은 헤드리스 브라우저(`browse` 스킬)로 수행한다. **헤드리스 프로필은 사용자 브라우저와 별개 → IndexedDB도 분리**되므로 헤드리스에서 새 PDF를 직접 업로드해야 한다(`pdf-office-verification-model` 메모리).

- [ ] **Step 1: 프로덕션 빌드 검증**

Run: `npm run build`
Expected: exit 0, 0 errors. (build 실패 시 해당 태스크로 돌아가 수정 후 재시도. `npm run lint`는 Next 16 사전 파손이라 게이트 제외.)

- [ ] **Step 2: 5페이지 검증용 PDF 생성 (헤드리스 업로드용)**

Run:
```bash
node -e "const{PDFDocument,StandardFonts,rgb}=require('pdf-lib');(async()=>{const d=await PDFDocument.create();const f=await d.embedFont(StandardFonts.HelveticaBold);for(let i=1;i<=5;i++){const p=d.addPage([595,842]);p.drawText('PAGE '+i,{x:60,y:760,size:48,font:f,color:rgb(0.1,0.1,0.1)});}const b=await d.save();require('fs').writeFileSync(require('os').tmpdir()+'/phase2-5p.pdf',b);console.log(require('os').tmpdir()+'/phase2-5p.pdf');})()"
```
Expected: 절대경로 1줄 출력(예: `C:\Users\...\Temp\phase2-5p.pdf`). 이 경로를 이후 업로드에 사용.

- [ ] **Step 3: 개발 서버 확인 후 헤드리스 도그푸딩**

개발 서버(`npm run dev`, 사용자 :3000 또는 신규 :3001)가 떠 있는지 확인한다. `browse` 스킬로 다음을 순서대로 검증하고 각 항목의 실측 결과를 기록한다(스크린샷/스냅샷):

1. **(spec §6-1) 2-up 그룹핑**: 앱 접속 → `section[aria-label="작업 영역"] input[type=file]`로 `phase2-5p.pdf` 업로드 → 뷰어 컨트롤바의 `Columns2`(두 페이지씩 보기) 버튼 클릭. 기대: 행이 `PAGE1·PAGE2 / PAGE3·PAGE4 / PAGE5`(2열, 마지막 단독), 각 페이지 ≈ 절반폭.
2. **(spec §6-2) 3모드 토글 + 영속**: `[연속]↔[단일]↔[2페이지]` 상호 전환 각 1회, 시각 회귀 0(레이아웃 깨짐·중복·빈 화면 없음). `2페이지` 상태로 새로고침 → spread 유지(persist v3 migrate가 'spread' 보존, 버전업 불요).
3. **(spec §6-3) spread 동작**: `2페이지`에서 스크롤 → 현재 페이지 번호·썸네일 동기화, 먼 슬롯은 썸네일/스켈레톤(윈도잉 동작 = IO viewMode deps 정상 입증), Ctrl+휠 줌 동작, 콘텐츠가 넓을 때 드래그 팬 동작.
4. **(spec §6-4) 패널 리사이즈**: 페이지 패널 우측 핸들(약 6px, `aria-label="페이지 패널 크기 조절"`)을 좌/우 드래그 → 폭 변경. 최소(180) 이하·최대(420) 초과로 드래그해도 클램프 확인. 새로고침 후 폭 영속. localStorage `pdf-office-pages-panel-w`를 `abc`(손상값)로 설정 후 새로고침 → 기본 248 폴백(앱 정상).
5. **(spec §6-5) 회귀 0**: `단일`·`연속` 모드 정상, 데스크탑 문서 사이드바 정상, 모바일 폭(예: 390px)에서 페이지 패널 `hidden`·드로어(LayoutGrid 토글) 정상.
6. **콘솔 에러 0**: 위 전 과정에서 콘솔 error 0건.

기대: 1~6 전부 통과. 실패 항목 발견 시 해당 태스크로 복귀해 수정 후 Step 1부터 재검증(증거 기반 완료 — 추측 금지).

- [ ] **Step 4: CLAUDE.md 변경 이력 행 추가**

`CLAUDE.md` "변경 이력" 표의 마지막(2026-05-17 Phase 1 행) 바로 아래에 행을 1개 추가한다.

추가할 행(표 마지막 `|` 행 다음 줄에 삽입):
```
| 2026-05-17 | 뷰어 UX Phase 2 — 2페이지(2-up) 스프레드 보기 모드·페이지 패널 드래그 리사이즈(localStorage 영속) | src/(types·spread·뷰어·셸·훅·리사이저)·docs | 사용자 피드백 2건(N행 2열 2페이지 보기·패널 폭 조정). 서브에이전트 주도(태스크별 스펙+코드품질 2단계 리뷰). groupIntoRows 순수 헬퍼, IO effect deps에 viewMode(모드 전환 윈도잉 정지 방지), 1열 동일인자로 continuous 회귀 0. tsc 0·build 0·헤드리스 도그푸딩 6/6. 후속(추적): scroll-sync per-frame querySelectorAll 성능 |
```

- [ ] **Step 5: 최종 커밋**

```bash
git add CLAUDE.md
git commit -m "docs: CLAUDE.md 변경 이력 — 뷰어 UX Phase 2 (2-up 스프레드·패널 리사이즈)"
```

(임시 PDF·localStorage 조작은 검증용이며 커밋 대상 아님. 푸시·배포·프로덕션 폴링은 사용자 별도 요청 시.)

---

## 자체 검토 (writing-plans self-review)

**1. Spec 커버리지** — spec 각 절을 태스크에 매핑:
- §4.1 보기 모드 확장: 타입(T1), ZoomControl 3번째 버튼(T3), PdfViewer 셸 분기(T4) ✓
- §4.2 2페이지 렌더(viewMode·cols·perCol·groupIntoRows·행 레이아웃·IO viewMode deps): T2(헬퍼)+T5(전체) ✓
- §4.3 패널 리사이즈(usePanelWidth·PanelResizer·AppShell 배선): T6+T7+T8 ✓
- §5 데이터 흐름(boxes useMemo viewMode deps, IO 재관찰): T5 Step4·Step6 ✓
- §6 검증 6항목: T9 Step1(build)·Step3(도그푸딩 1~6) ✓
- §7 리스크(IO stale/2열 초과/리사이즈 텍스트선택/localStorage 손상/1열 회귀): T5 Step6·T5 Step4 perCol·T7 user-select·T6 Number.isFinite·T5 동일인자 ✓
- §8 파일 변경 맵 8개 파일: types(T1)·spread(T2)·ContinuousViewer(T5)·PdfViewer(T4)·ZoomControl(T3)·usePanelWidth(T6)·PanelResizer(T7)·AppShell(T8) — 전부 커버, "무변경" 명시 파일(PageSlot·useViewerScrollSync·doc-cache·page-box·renderer·SinglePageViewer·store) 미수정 ✓

갭 없음.

**2. 플레이스홀더 스캔** — "TBD/TODO/적절히 처리/나중에 구현/Task N과 유사" 없음. 모든 코드 스텝은 완전 코드 블록·정확 경로·정확 명령·기대 출력 포함. 검증 모델은 프로젝트 표준(테스트 러너 없음)을 명시적으로 따르며 순수 함수 런타임 정확성을 T9 도그푸딩 1번으로 입증하도록 연결(공백 아님). ✓

**3. 타입 일관성** — `ViewMode`(T1) = `'continuous'|'single'|'spread'`. `groupIntoRows<T>(items:T[],cols:number):T[][]`(T2) ↔ T5에서 `groupIntoRows(pages.map((_,i)=>i), cols)`로 `T=number` 사용 일치. `usePanelWidth(key,def,min,max):{width,setWidth}`(T6) ↔ T8 `usePanelWidth('pdf-office-pages-panel-w',248,180,420)` 인자수·순서·구조분해(`width/setWidth`) 일치. `PanelResizer` props `{width,min,max,onWidthChange}`(T7) ↔ T8 전달 prop 4개 일치. `SPREAD_GAP=16`(T5 Step2) ↔ perCol 식(T5 Step4) 동일 식별자. boxes deps `viewMode`(T5 Step4) ↔ IO deps `viewMode`(T5 Step6) 일관. ✓

이상 — 수정 사항 없음.
